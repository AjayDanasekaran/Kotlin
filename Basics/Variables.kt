fun main()  {
    var s = 5;
    println(s);
    s=7;
    println(s);
    // val b = 4;
    // println(b);
    //b = 3; val cannot be reassigned
    // println(b)

    var score : Int = 0;
    score = 100;   
    score =3.4.toInt(); // Explicit Typecasting
    println(score);
}